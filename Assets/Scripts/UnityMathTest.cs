using UnityEngine;

public class UnityMathTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField]
    public DM64 testValue;
    [SerializeField]
    public DMVector testVector;

}
